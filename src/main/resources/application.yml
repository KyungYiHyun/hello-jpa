spring:
  datasource:
    url: jdbc:mysql://localhost:3306/jpashop?&useSSL=false&allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=UTC
    username: root
    password: '0000'
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect : org.hibernate.dialect.MySQL8Dialect
    show-sql: true
    format-sql: true